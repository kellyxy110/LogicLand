// Algebra Studio — expand, factor, simplify and solve, with each step explained (MathLab, ADR-028).
import { AlgebraStudio } from "@/features/mathlab/AlgebraStudio";

export const metadata = {
  title: "Algebra Studio · LogicLand",
  description: "Manipulate expressions with confidence — expand, factor, simplify and solve, with each step explained.",
};

export default function AlgebraStudioPage() {
  return <AlgebraStudio />;
}
